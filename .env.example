# Base URL of your existing Node/Express backend.
# Never put database URIs, JWT secrets, or third-party API keys here —
# anything in a Vite .env prefixed with VITE_ is bundled into the browser
# JS and publicly visible. All secrets stay server-side in your backend's
# own .env.
VITE_API_BASE_URL=http://localhost:5000
